log:
  level: warn
  loggers:
    it.imolainformatica:
      level: debug

  appenders:
    - type: console
      logFormat: '%d [%thread] %-5level %logger{35} - %msg%n'
      target: stdout

camel:
  requiresHttpProcessor: true